CREATE DATABASE IF NOT EXISTS academic_management;
USE academic_management;

-- The tables will be automatically created by Hibernate (spring.jpa.hibernate.ddl-auto=update)
-- but here are some initial data inserts if needed.

-- Example: Default Admin
-- INSERT INTO users (email, firebase_uid, full_name, role) VALUES ('admin@example.com', 'admin-uid', 'Platform Administrator', 'ADMIN');
